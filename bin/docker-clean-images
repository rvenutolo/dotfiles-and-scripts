#!/usr/bin/env bash

set -euo pipefail

if [[ "$#" -ne 0 ]]; then
  echo "${0##*/}: Expected no arguments" >&2
  exit 2
fi

readonly images="$(docker images -a --filter=dangling=true -q)"
if [[ -n "${images}" ]]; then
  docker rmi "${images}"
fi
