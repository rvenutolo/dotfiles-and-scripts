#!/usr/bin/env bash

set -euo pipefail

if [[ "$#" -ne 0 ]]; then
  echo "${0##*/}: Expected no arguments" >&2
  exit 2
fi

readonly containers="$(docker ps --filter=status=exited --filter=status=created -q)"
if [[ -n "${containers}" ]]; then
  docker rm "${containers}"
fi
