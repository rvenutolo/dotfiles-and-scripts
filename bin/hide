#!/usr/bin/env bash

set -euo pipefail

if [[ "$#" -eq 0 ]]; then
  readonly script_name="${0##*/}"
  echo "${script_name}: Expected at least 1 argument" >&2
  exit 2
fi

for file in "$@"; do
  mv -v "${file}" "$(dirname "${file}")"/."$(basename "${file}")" || exit 1
done
