#!/usr/bin/env bash

set -euo pipefail

if [[ "$#" -ne 1 ]]; then
  echo "${0##*/}: Expected exactly 1 argument" >&2
  exit 2
fi

case "$1" in
  *.md) glow -s dark "$1" | less -r ;;
  *) highlight -O ansi "$1" --force | less -r ;;
esac
