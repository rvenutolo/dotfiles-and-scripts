#!/usr/bin/env bash

set -euo pipefail

readonly MAVEN='${SDKMAN_DIR}/candidates/maven/current/bin/mvn'
readonly MAVEN_WRAPPER='./mvnw'

if [[ -f ${MAVEN_WRAPPER} ]]; then
   maven_cmd=${MAVEN_WRAPPER}
else
   maven_cmd=${MAVEN}
fi

exec ${maven_cmd} "$@"
